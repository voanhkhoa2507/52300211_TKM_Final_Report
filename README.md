# 52300211_TKM_Final_Report

Báo cáo + mã nguồn mô phỏng đồ án **Metro Ethernet MAN dùng MPLS** (Mininet/FRR) kết nối **3 chi nhánh** (Flat / 3-layer / Leaf-Spine).

## Cấu trúc thư mục

- `source/`
  - `topology.py` 
  - `tool.py` 
- `image/` (file topology draw.io và ảnh/biểu đồ output)

